# Build Scope

- Current release/build target policy: **iOS only**.
- Do not initiate Android builds in this project unless explicitly requested.
- Use iOS builds only (`pnpm exec eas build --platform ios --profile development`), and use interactive/credentialed workflows as needed.
